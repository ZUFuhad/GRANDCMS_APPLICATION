import Shell from '@/src/components/Shell'; import {requireUser} from '@/src/lib/auth';
export default async function Page(){await requireUser();return <Shell title="Expenses"><div className="card"><p>Expense tracking and project/client cost allocation.</p><div className="row"><a className="btn" href="#create">Add new</a><a className="btn alt" href="#list">View records</a></div></div><div id="create" className="card" style={{marginTop:16}}><h2>Quick entry</h2><form className="form"><input placeholder="Name / title"/><input placeholder="Email / phone"/><textarea placeholder="Notes / description"/><button className="btn" type="button">Save (API-ready)</button></form></div></Shell>}
