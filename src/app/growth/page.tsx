import Shell from '@/src/components/Shell'; import {requireUser} from '@/src/lib/auth';
export default async function Page(){await requireUser();return <Shell title="Agentic Growth"><div className="card"><h2>Client Discovery</h2><form className="form" action="/api/growth/discover" method="post"><input name="objective" placeholder="e.g. Find corporate event prospects in Chattogram"/><textarea name="criteria" placeholder="Industry, location, company size, services needed"></textarea><button className="btn">Run discovery</button></form><p className="muted">The adapter requires an external research/LLM provider to return verified public-source leads; no lead is fabricated.</p></div></Shell>}
