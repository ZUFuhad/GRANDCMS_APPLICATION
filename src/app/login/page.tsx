'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const r=useRouter();async function submit(e:any){e.preventDefault();const x=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(x.ok)r.push('/dashboard');else setError((await x.json()).error||'Login failed')}return <div className="login"><form className="loginbox form" onSubmit={submit}><h1>Grand CMS</h1><p className="muted">Secure company management system</p>{error&&<div className="error">{error}</div>}<input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="btn">Sign in</button></form></div>}
