import {requireUser} from '@/src/lib/auth'; import {db} from '@/src/lib/db'; import {quoteDoc} from '@/src/lib/documents';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){await requireUser();const {id}=await params;const q=await (await db()).quotation.findUnique({where:{id},include:{client:true,items:true}});if(!q)return new Response('Not found',{status:404});const buf=await quoteDoc(q);return new Response(buf as any,{headers:{'Content-Type':'application/vnd.openxmlformats-officedocument.wordprocessingml.document','Content-Disposition':`attachment; filename="${q.number}.docx"`}})}
